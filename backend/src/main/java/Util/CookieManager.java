package Util;

import Users.Participant;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

public class CookieManager {

    /**
     * Method receives an action, a participant, and a request/response, and handles the cooking setting, fetching, or
     * updating, depending on the case
     * @param request ServletRequest from which to verify if cookies exist
     * @param response ServletResponse to which we send or update the cookie to
     */
    public static void handleParticipantAction(HttpServletRequest request, HttpServletResponse response){

    }

}
